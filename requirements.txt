# Python RNA-seq Pipeline Requirements
# This file lists Python dependencies only
# Bioinformatics tools should be installed via conda or containers

# No additional Python packages required beyond standard library
# The pipeline uses subprocess to call external tools:
# - fastqc
# - STAR
# - samtools
# - featureCounts (subread)
